<module id="mds-quota">
    <name>User quota support</name>
    <desc>Set file quotas on the file server</desc>
    <packages>
        <target name="all">
            <rpm>quota</rpm>
            <rpm>python-mmc-userquota</rpm>
            <rpm>mmc-web-userquota</rpm>
        </target>
    </packages>
    <deps>
        <module>mds-mmc</module>
    </deps>
    <config>
        <text name="filesystems" require="yes" default="/dev/sda6:Home" multi="yes">
            <label>Filesystems</label>
            <help>Specify the filesystems where you want to apply user quotas with the following format : "Device path":"Identifier string" (eg: /dev/sda1:Root)</help>
        </text>
    </config>
</module>
