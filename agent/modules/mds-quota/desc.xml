<module id="mds-quota">
    <name>User quota support</name>
    <desc>Set file quotas on the file server</desc>
    <packages>
        <target name="all">
            <rpm>quota</rpm>
            <rpm>python-mmc-userquota</rpm>
            <rpm>mmc-web-userquota</rpm>
        </target>
    </packages>
    <deps>
        <module>mds-mmc</module>
    </deps>
    <config>
        <text name="filesystems" require="yes" default="Home:/dev/sda6" multi="yes">
            <label>Filesystems</label>
            <help>Specify the filesystems where you want to apply user quotas with the following format : Identifier string:Device path (eg: Root:/dev/sda1)</help>
        </text>
    </config>
</module>
