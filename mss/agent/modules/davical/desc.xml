<?xml version="1.0" encoding="UTF-8"?>
<module xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:noNamespaceSchemaLocation="../desc.xsd" id="davical">
    <name>Calendar and Addressbook</name>
    <desc>DAViCal a service for calendar and addressbook sharing.</desc>
    <actions>
        <action type="link" name="DAViCal administration" value="https://@HOSTNAME@/davical/" />
    </actions>
    <packages>
        <target name="all">
            <rpm>davical</rpm>
            <!-- The following dependences should be inside the package. While the dependency is broken, let's install it manually -->
            <rpm>php-pdo_pgsql</rpm>
            <rpm>php-calendar</rpm>
            <rpm>php-ldap</rpm>
        </target>
    </packages>
    <dependencies>
        <module>postgresql</module>
    </dependencies>
</module>
