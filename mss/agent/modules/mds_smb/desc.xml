<module xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:noNamespaceSchemaLocation="../desc.xsd" id="mds_smb">
    <name>Windows users and file sharing management</name>
    <desc>Authenticate Windows users with SAMBA and provide Windows file shares on your network</desc>
    <actions>
        <action type="link" name="Management interface" value="https://@HOSTNAME@/mmc/main.php" />
    </actions>
    <packages>
        <target name="all">
            <rpm>samba-server</rpm>
            <rpm>samba-client</rpm>
            <rpm>samba-common</rpm>
            <rpm>samba-doc</rpm>
            <rpm>smbldap-tools</rpm>
            <rpm>mmc-web-samba</rpm>
            <rpm>python-mmc-samba</rpm>
        </target>
    </packages>
    <deps>
        <module>mds_mmc</module>
    </deps>
    <config>
        <text name="smbdomain" require="yes" default="MBSDOMAIN"
            validation="valid_domain">
            <label>SAMBA domain name</label>
            <help>The name of your Microsoft domain.</help>
        </text>
        <text name="smbnetbios" require="yes" default="get_netbios"
            validation="valid_netbios">
            <label>Netbios name</label>
            <help>The name of your SAMBA server in the Microsoft domain.</help>
        </text>
        <text name="smbadmin" require="yes" default="admin">
            <label>Administrator name</label>
            <help>The name of the Microsoft domain administrator.</help>
        </text>
        <password name="smbpasswd" require="yes">
            <label>Administrator password</label>
            <help>Password for the Microsoft domain administrator.</help>
        </password>
        <check name="fw_lan" default="on">
            <label>Allow connections from internal networks</label>
            <help>Configure the firewall to accept SAMBA related traffic on interfaces
                configured as 'internal'</help>
        </check>
        <check name="fw_wan" default="off">
            <label>Allow connections connections external networks</label>
            <help>Configure the firewall to accept SAMBA related traffic on interfaces
                configured as 'external'</help>
        </check>
    </config>
</module>
