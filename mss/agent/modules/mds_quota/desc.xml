<module xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:noNamespaceSchemaLocation="../desc.xsd" id="mds_quota">
    <name>User quota management</name>
    <desc>Set file quotas on the server</desc>
    <packages>
        <target name="all">
            <rpm>quota</rpm>
            <rpm>python-mmc-userquota</rpm>
            <rpm>mmc-web-userquota</rpm>
        </target>
    </packages>
    <deps>
        <module>mds_mmc</module>
    </deps>
    <config>
        <text name="filesystems" require="yes" default="get_filesystems" multi="yes">
            <label>Filesystems</label>
            <help>Specify the filesystems where you want to apply user quotas with the following format : "Device path":"Identifier string" (eg: /dev/sda1:Root)</help>
        </text>
    </config>
</module>
