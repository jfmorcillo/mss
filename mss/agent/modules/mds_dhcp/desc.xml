<module xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:noNamespaceSchemaLocation="../desc.xsd" id="mds_dhcp">
    <name>DHCP service</name>
    <desc>Configure automatically your client computers IP settings</desc>
    <actions>
        <action type="link" name="Management interface" value="https://@HOSTNAME@/mmc/main.php" />
    </actions>
    <packages>
        <target name="all">
            <rpm>dhcp-server</rpm>
            <rpm>mmc-web-network</rpm>
            <rpm>python-mmc-network</rpm>
        </target>
    </packages>
    <deps>
        <module>mds_mmc</module>
    </deps>
    <config>
        <check name="fw_lan" default="on">
            <label>Allow DHCP requests from internal networks</label>
            <help>Configure the firewall to accept DHCP requests on interfaces
                configured as 'internal'</help>
        </check>
        <check name="fw_wan" default="off">
            <label>Allow DHCP requests from external networks</label>
            <help>Configure the firewall to accept DHCP requests on interfaces
                configured as 'external'</help>
        </check>
    </config>
    <text>Configuration done. (/etc/mmc/plugins/network.ini updated)</text>
    <text>Error while configuring the dhcp service. (/etc/mmc/plugins/network.ini)</text>
    <text>Configuration of dhcp-server done. (/etc/dhcpd.conf updated)</text>
    <text>Error while configuring dhcp-server. (/etc/dhcpd.conf)</text>
    <text>Service mmc-agent reloaded succesfully.</text>
    <text>Service mmc-agent fails restarting. Check /var/log/mmc/mmc-agent.log</text>
    <text>The DHCP service is installed.</text>
    <text>You can now configure your DHCP settings from the management interface : https://@HOSTNAME@/mmc/</text>
</module>
